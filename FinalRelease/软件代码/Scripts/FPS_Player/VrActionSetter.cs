using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// using Valve.VR;

public class VrActionSetter : MonoBehaviour
{

    // public SteamVR_ActionSet primarySet;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if(Input.GetKeyDown(KeyCode.Space) && primarySet != null)
        // {
        //     bool active = primarySet.IsActive();
        //     active = !active;

        //     if(active)
        //         primarySet.Activate(SteamVR_Input_Sources.Any, 10, true);
        //     else
        //         primarySet.Deactivate();
        // }
    }
}
